package LambdasDemo;

//Functional interface because it represents a function, one single method in the interface. We use Lambda
// expressions to represent this interfaces

public interface Printer {
    void print (String message);
}
