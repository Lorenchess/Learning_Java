package OOP_Linkedin;
//Enum is a special type that represents a group of constants...These are the only values we can give to a variable
// with the tree data type. By using here the enum no one could create a "macaroony tree" tht would be the case if we
// use a String type.

public enum TreeType {
    OAK,
    MAPLE,
    PECAN,
    WALNUT,
    PINE,
}
